FROM python:3.12-slim

RUN apt-get update && apt-get install -y \
    ghostscript imagemagick libreoffice \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .
ENV PORT=5000
CMD ["python", "app.py"]